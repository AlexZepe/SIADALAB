{% extends 'base.html.twig' %}

{% block body %}
{{ parent() }}


<div class="row">
  <div class="col-md-2"></div>
  <div class="col-md-8">
      




<h1>Roles</h1>
<div class="pull-right"> <a href="{{ path('rol_new') }}" class="btn btn-info">Crear Nuevo Rol</a> </div>
<div class="clearfix"></div>


<br />
<br />


    



    <table class="table table-hover text-center">
        <thead>
            <tr>
                <th class="text-center">Id</th>
                <th class="text-center">Rol</th>
                <th class="text-center">Descripción</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for rol in rols %}
            <tr>
                <td><a href="{{ path('rol_show', { 'id': rol.idRol }) }}">{{ rol.idRol }}</a></td>
                <td>{{ rol.nomRol }}</td>
                <td>{{ rol.descripcionRol }}</td>
                <td>
                    <a href="{{ path('rol_show', { 'id': rol.idRol }) }}" class="btn btn-sm btn-primary">Ver</a>
                    <a href="{{ path('rol_edit', { 'id': rol.idRol }) }}" class="btn btn-sm btn-success">Editar</a>
                    <a href="#" class="btn btn-sm btn-danger">Eliminar</a>
                            
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  </div>
  <div class="col-md-2"></div>
</div>


    

{% endblock %}
